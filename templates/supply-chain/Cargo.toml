[package]
name = "supply_chain_zome"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"

[lib]
crate-type = ["cdylib", "rlib"]
name = "supply_chain_zome"

[dependencies]
adk = { path = "../../crates/adk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
mock = ["adk/mock"]
